# 作品配置与主题验证

日期：2026-09-21。SDK `a3d5bcfa0f846cc06b481b582236069be18e1059fd4243d7dcf9bd5d714ad837`；测试工程发行 `01fb072df51c9c8cd6c75a66e3a01b93b2469552dc28cb334435da5962669d05`，12 个对象，6,826,037 字节，其中 WASM 3,964,074 字节。

环境为 Linux、Chromium 153.0.8010.52、X11/Xvfb，SwiftShader 软件 WebGPU。真实浏览器执行实际 WASM 和 wgpu，不代表物理 GPU、移动真机或声音可听性认证。开发机路径已替换成 `/nir`、`/build-home`。

| 检查 | 结果 |
|---|---|
| 原生/呈现 | 69 项通过（xtask 68，呈现 1）；最后的颜色/投影调整另有定向复验 |
| 宿主 | 20 项通过，包括首次设置、系统减少动态与玩家偏好优先 |
| 浏览器 | 19 项通过，0 失败、跳过、flaky；115.9 秒 |
| fmt、原生/WASM lint、依赖边界 | 通过；wgpu vendor 的两项既有警告未修改 |
| 独立 SDK | 不依赖 Cargo 的创建、校验、剧情测试、构建、配置报告通过 |
| 可复现/发行检查 | 同输入重复构建一致；锁漂移拒绝；运行对象哈希正确 |

新增浏览器用例使用独立创建的作品，仅修改主题与 player 配置，保持 SDK 文件及作品锁不变，以 `--locked` 构建最终静态目录并在子路径加载。验证顶部对白 y=64、窄屏选项宽度 350、紧凑行距 64，使用实际键盘焦点和 Enter 选择，走完 walk 结局。新作品字号 1.2/音乐音量 0.12/减少动态可用；玩家将字号改为 1.3，返回标题、刷新后仍保留。既有回归覆盖两条路线、存读档、语言切换、错误重试和设备重建。

原生新增用例覆盖配置来源、未知槽/错误组件/未知字段、越界与 NaN、路径逃逸、不支持的预设、旧 token JSON、手写运行 Program 的再验证、同 SDK 改主题发行改变但重复构建一致。投影测试覆盖 1280×800、390×844、844×390 三种尺寸与最大玩家字号，Gate 和禁用选项不能被主题覆盖，菜单出口保留。自动阅读测试验证作者基础等待与菜单暂停语义。

一次初次浏览器运行在新增用例中错误地等待了播放期间的所有请求归零；循环 BGM 合法保留音频完成槽。测试改为返回标题后等待存储完成，再刷新验证，随后完整重跑 19 项通过。没有改变运行时的请求生命周期来满足这个错误条件。

本阶段未重跑重复性能基线，不能据此宣称帧性能改善；上一阶段的软件适配器帧间隔未达标与物理 GPU 未验收结论仍成立。任意长文本、多选项滚动、通用页面/主题资源、独立正文与 UI 语言配置仍未实现。

证据：[原生](theme-native.log)、[呈现](theme-presentation.log)、[投影复验](theme-projection.log)、[颜色复验](theme-contrast.log)、[原生 lint](theme-lint.log)、[WASM lint](theme-wasm-lint.log)、[宿主](theme-host.log)、[浏览器](theme-browser.log)、[机器结果](browser-results.json)、[独立 SDK](theme-standalone.json)、[发行](theme-release.json)、[配置来源](theme-resolved.json)。

实际截图：[顶部对白](theme-top-dialogue.png)、[窄屏紧凑选项](theme-compact-choice.png)。
